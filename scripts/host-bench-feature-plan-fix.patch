diff --git a/docs/benchmark-method.md b/docs/benchmark-method.md
index 1b91ff6..e2c3c28 100644
--- a/docs/benchmark-method.md
+++ b/docs/benchmark-method.md
@@ -244,27 +244,40 @@ the on-device suites:
   (Qwen3.6: depth 135 vs 64 planes). The engine refuses honestly and full-clears; correctness is
   never at risk.
 
-### Feature A/B matrix: which model measures which knob
+### Feature A/B coverage: what is measured, and what the per-model cells still owe
 
-The matrix above measures the engine's baseline thesis (streaming vs mmap). The engine also ships
-a compute/latency-hiding layer that the per-model cells do not touch: speculative decode
-(`--mtp`, `--ngram`), cache-aware routing (`--expert-substitute`, `--drop-cold-experts`,
-`--drop-in-prefill`, `--route-ahead`), I/O timing (`--io-two-wave`, `--prefetch`), and the
-serve/load variants (`--row-stream`, `--dense-odirect`, `--release-mmap`). Each gets its A/B on
-the model that isolates its mechanism with the least confound; where two models give the same
-insight, the faster one runs first. All of these are our own implementations except the ngram
-draft source (mainline `ngram-cache`, consumed by our verify loop).
+The per-model cells above measure the engine's baseline thesis (streaming vs mmap). The engine also
+ships a compute/latency-hiding layer — and most of it is **already measured in its own feature doc**
+with committed evidence under `docs/bench-data/`. Do not re-run those; read the doc. Only the
+unmeasured remainder gets an A/B here, paired to the model that isolates its mechanism (faster
+model first when the insight is equal). Provenance: everything below is our own implementation
+except the ngram draft *source* (mainline `ngram-cache`, consumed by our verify loop).
+
+Measured, on record:
+
+| Feature | Doc | Models on record |
+|---|---|---|
+| `--mtp` | [mtp.md](mtp.md) | Qwen3.5 / Qwen3.6 (nextn ggufs, MXFP4 + Q4), incl. the determinism section |
+| `--ngram` | [ngram.md](ngram.md) | Qwen3-30B, Qwen3.6 |
+| `--expert-substitute` | [cache-aware-substitution.md](cache-aware-substitution.md) + `bench-data/2026-08-26-*` | Qwen3.6, with HumanEval50/TinyMMLU quality gates |
+| `--drop-cold-experts` | [expert-dropping.md](expert-dropping.md) | in the published host recipes (`0.75`) |
+| `--prefetch` | [prefetch.md](prefetch.md) | gpt-oss — measured **negative**, premise refuted |
+| `--predict-prefetch` | [expert-prediction.md](expert-prediction.md) | Qwen3-30B, Qwen3.6 on device — full-routing speculation −38 %, refuted as a win |
+| `--route-ahead` | [route-ahead.md](route-ahead.md) | N=2, lane sweep (+21 % in its table) |
+| `--row-stream` | [row-gathered-tables.md](row-gathered-tables.md) | embedding-table rows, on device |
+
+Unmeasured or cell-gapped, with the pairing (2026-09-20 session results marked DONE):
 
 | Priority | Model (profile) | Feature A/B | Insight sought |
 |---|---|---|---|
-| 1 | OLMoE (fastest, fits RAM) | `--ngram` (standard + code-repetition prompt) | pure speculation gain, zero thrash confound; ngram feeds on prompt repetition, so the edit/code workload is its habitat |
-| 2 | OLMoE | `--dense-odirect`, `--ubatch` | cheapest runs; dense reads are its only remaining I/O, so the delta is unambiguous; fills the ubatch-512 protocol-justification row |
-| 3 | LFM2.5 (hybrid, fits RAM) | `--ngram` | does speculative verify compose with recurrent-state rollback planes? |
-| 4 | Ling-mini (marginal fits-RAM) | `--row-stream` | can serve-time dense row streaming shrink the resident sum and recover the −31 % penalty? |
-| 5 | Cyber-Tiel (MTP carrier) | `--mtp` | the only model with MTP heads; the only multiplicative lever we've built; A/B vs its cell (b), quality column alongside per the lossy rule |
-| 6 | Cyber-Tiel (highest miss headroom) | `--expert-substitute`, `--drop-cold-experts`, `--drop-in-prefill`, `--route-ahead` | ~18 % cache misses to win back on the worst-case model; report `experts_substituted`/`experts_dropped`/`experts_routed` with the tok/s |
-| 7 | Qwen3.6 (deepest thrash) | `--io-two-wave`, `--prefetch` | maximum stall signal (533 majflt/tok); short runs — the metrics are majflt/stall, not answer length; `--release-mmap` load-peak metric rides along |
-| 8 | Cyber-Tiel | `--predict-prefetch` (+`--predict-spec-max`) | composition test: prefetch driven by speculated tokens; only after the MTP row exists |
-
-Cyber-Tiel rows 5/6/8 share one load session (load is the expensive part at 21 GB). Lossy knobs
-report their quality evidence in the same row — that is the rule, not a preference.
+| 1 — DONE | OLMoE (fastest, fits RAM) | `--ubatch` 256/1024 | −0.9 % / −1.7 %: **512 confirmed** as the protocol default, with numbers |
+| 2 — DONE | OLMoE | `--dense-odirect` | −0.6 %: neutral on fits-RAM (dense barely read post-warmup) |
+| 3 — DONE | OLMoE | `--ngram`, prose vs repetition prompt | prose **−4.3 %** (5.6 % coverage); repetition **+3.3 %** (64.5 % accept, 1.45 tok/verify) — works when fed, CPU verify cost caps it |
+| 4 | LFM2.5 (hybrid) | `--ngram` | does speculative verify compose with recurrent-state rollback planes? |
+| 5 | Cyber-Tiel (MTP carrier) | `--mtp` on the exact matrix cell (Q4_K_M) | mtp.md measured MXFP4/other files; the 21 GB carrier cell itself is unrowed |
+| 6 | Cyber-Tiel (81.9 % hit) | `--expert-substitute`, `--drop-in-prefill` | substitution doc used Qwen3.6; headroom check on the carrier's ~18 % misses |
+| 7 | Qwen3.6 (deepest thrash) | `--io-two-wave` | the one I/O-timing knob with no table anywhere |
+| 8 | any | `--release-mmap` | load-peak metric only; not a tok/s row |
+
+Lossy knobs report their quality evidence in the same row — that is the rule, not a preference.
+Rows 5/6 share one Cyber-Tiel load session (load is the expensive part at 21 GB).
